# GitHub Gist 云端同步 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为英语学习 PWA 添加 GitHub Gist 云端备份同步，支持手动推拉、可选自动推送和 URL 参数引导配置。

**Architecture:** 扩展 settingsStore 存储 GistConfig，新建 `src/lib/gist.ts` 纯网络层（复用现有 `buildBackup/applyBackup`），新建 `src/hooks/useGistUrlParams.ts` 处理 URL 参数冲突，Settings 页新增 Gist 同步 Section（使用 frontend-design skill 设计 UI）。

**Tech Stack:** React 18, Zustand, Dexie (IndexedDB), GitHub REST API v3, Tailwind CSS, lucide-react

---

## 文件变更清单

| 文件 | 操作 |
|------|------|
| `src/stores/settingsStore.ts` | 修改：新增 GistConfig 类型、DEFAULT_GIST、gist 状态与 setGist action |
| `src/lib/gist.ts` | 新建：GitHub Gist REST API 封装 |
| `src/hooks/useGistUrlParams.ts` | 新建：URL 参数解析与冲突处理 hook |
| `src/components/AppLayout.tsx` | 修改：挂载时调用 useGistUrlParams |
| `src/pages/Settings.tsx` | 修改：新增 Gist 同步 Section + TOC 条目（UI 使用 frontend-design skill） |
| `src/pages/Learn.tsx` | 修改：学习完成时调用 autoSyncToGist |
| `src/pages/Review.tsx` | 修改：复习批次完成时调用 autoSyncToGist |

---

## Task 1: 扩展 settingsStore — GistConfig 类型与状态

**Files:**
- Modify: `src/stores/settingsStore.ts`

- [ ] **Step 1: 在 settingsStore.ts 中新增 GistConfig 接口和默认值**

在文件顶部类型定义区（`TTSConfig` 之后）添加：

```typescript
export interface GistConfig {
  gistId: string;
  token: string;
  autoSync: boolean;
  includeAIConfig: boolean;
  lastSyncedAt?: string;
  lastSyncStatus?: 'ok' | 'error';
}

export const DEFAULT_GIST: GistConfig = {
  gistId: '',
  token: '',
  autoSync: false,
  includeAIConfig: false,
};
```

- [ ] **Step 2: 在 SettingsState 接口中新增 gist 字段和 setGist action**

在 `SettingsState` 接口的 `loaded` 字段之前添加：

```typescript
  gist: GistConfig;
  setGist: (cfg: Partial<GistConfig>) => Promise<void>;
```

- [ ] **Step 3: 在 create() 初始状态中新增 gist 初始值**

在 `loaded: false,` 之前添加：

```typescript
  gist: DEFAULT_GIST,
```

- [ ] **Step 4: 在 load() 中追加 gist 读取**

在 `load()` 的 `Promise.all` 数组中追加 `getSetting<GistConfig>('gist', DEFAULT_GIST)`，并在 `set({...})` 中追加：

```typescript
gist: { ...DEFAULT_GIST, ...gist },
```

完整修改后的 `load()` 中 Promise.all：

```typescript
const [lvl, dnw, drl, order, algo, sfx, tts, ai, mode, enh, gist] = await Promise.all([
  getSetting<LevelId>('activeLevel', 'junior'),
  getSetting<number>('dailyNewWords', 20),
  getSetting<number>('dailyReviewLimit', 100),
  getSetting<LearnOrder>('learnOrder', 'sequential'),
  getSetting<ReviewAlgorithm>('reviewAlgorithm', 'sm2'),
  getSetting<boolean>('sfxEnabled', false),
  getSetting<TTSConfig>('tts', DEFAULT_TTS),
  getSetting<AIConfig>('ai', DEFAULT_AI),
  getSetting<LearningMode>('learningMode', 'classic'),
  getSetting<EnhancedConfig>('enhanced', DEFAULT_ENHANCED),
  getSetting<GistConfig>('gist', DEFAULT_GIST),
]);
set({
  activeLevel: lvl,
  dailyNewWords: dnw,
  dailyReviewLimit: drl,
  learnOrder: order,
  reviewAlgorithm: algo,
  sfxEnabled: sfx,
  tts: { ...DEFAULT_TTS, ...tts },
  ai: { ...DEFAULT_AI, ...ai },
  learningMode: mode,
  enhanced: { ...DEFAULT_ENHANCED, ...enh },
  gist: { ...DEFAULT_GIST, ...gist },
  loaded: true,
});
```

- [ ] **Step 5: 新增 setGist action 实现**

在 `setEnhanced` action 之后添加：

```typescript
async setGist(cfg) {
  const next = { ...get().gist, ...cfg };
  await setSetting('gist', next);
  set({ gist: next });
},
```

- [ ] **Step 6: 验证 TypeScript 编译通过**

```bash
cd "D:/project/5_AITools/english"
npx tsc --noEmit
```

Expected: 无错误（或仅有已有的告警）

- [ ] **Step 7: Commit**

```bash
git add src/stores/settingsStore.ts
git commit -m "feat(gist): add GistConfig type and state to settingsStore"
```

---

## Task 2: 新建 Gist 网络层 `src/lib/gist.ts`

**Files:**
- Create: `src/lib/gist.ts`

- [ ] **Step 1: 创建 gist.ts 文件**

```typescript
import { buildBackup, parseBackup, type BackupV1, type ExportOptions } from './backup';
import type { GistConfig } from '@/stores/settingsStore';

const GIST_FILENAME = 'english-hub-backup.json';
const GIST_DESCRIPTION = 'English Hub Backup';
const API_BASE = 'https://api.github.com';

function headers(token: string) {
  return {
    'Authorization': `Bearer ${token}`,
    'Content-Type': 'application/json',
    'Accept': 'application/vnd.github+json',
    'X-GitHub-Api-Version': '2022-11-28',
  };
}

export async function pushToGist(backup: BackupV1, config: GistConfig): Promise<string> {
  const content = JSON.stringify(backup, null, 2);
  const body = {
    description: GIST_DESCRIPTION,
    public: false,
    files: { [GIST_FILENAME]: { content } },
  };

  const isNew = !config.gistId;
  const url = isNew
    ? `${API_BASE}/gists`
    : `${API_BASE}/gists/${config.gistId}`;
  const method = isNew ? 'POST' : 'PATCH';

  const res = await fetch(url, { method, headers: headers(config.token), body: JSON.stringify(body) });
  if (!res.ok) {
    const err = await res.json().catch(() => ({}));
    throw new Error(`GitHub API ${res.status}: ${(err as any).message ?? res.statusText}`);
  }
  const data = await res.json();
  return data.id as string;
}

export async function pullFromGist(config: GistConfig): Promise<BackupV1> {
  const res = await fetch(`${API_BASE}/gists/${config.gistId}`, {
    headers: headers(config.token),
  });
  if (!res.ok) {
    const err = await res.json().catch(() => ({}));
    throw new Error(`GitHub API ${res.status}: ${(err as any).message ?? res.statusText}`);
  }
  const data = await res.json();
  const file = data.files?.[GIST_FILENAME];
  if (!file) throw new Error(`Gist 中不存在文件 ${GIST_FILENAME}`);
  const rawUrl = file.raw_url as string;
  // 内容超过 1MB 时 GitHub 不内联，需走 raw_url
  const raw = file.truncated
    ? await fetch(rawUrl).then((r) => r.text())
    : (file.content as string);
  return parseBackup(raw);
}

export async function getGistMeta(config: GistConfig): Promise<{ updatedAt: string }> {
  const res = await fetch(`${API_BASE}/gists/${config.gistId}`, {
    headers: headers(config.token),
  });
  if (!res.ok) throw new Error(`GitHub API ${res.status}`);
  const data = await res.json();
  return { updatedAt: data.updated_at as string };
}

export async function autoSyncToGist(
  gist: GistConfig,
  setGist: (cfg: Partial<GistConfig>) => Promise<void>,
): Promise<void> {
  if (!gist.autoSync || !gist.token) return;
  try {
    const exportOpts: ExportOptions = {
      includeSettings: true,
      includeSessions: true,
      includeWords: false,
      includeAIConfig: gist.includeAIConfig,
    };
    const backup = await buildBackup(exportOpts);
    const gistId = await pushToGist(backup, gist);
    await setGist({
      gistId,
      lastSyncedAt: new Date().toISOString(),
      lastSyncStatus: 'ok',
    });
  } catch {
    await setGist({ lastSyncStatus: 'error' });
  }
}
```

- [ ] **Step 2: 验证 TypeScript 编译通过**

```bash
npx tsc --noEmit
```

Expected: 无错误

- [ ] **Step 3: Commit**

```bash
git add src/lib/gist.ts
git commit -m "feat(gist): add GitHub Gist REST API layer"
```

---

## Task 3: 新建 URL 参数解析 Hook `src/hooks/useGistUrlParams.ts`

**Files:**
- Create: `src/hooks/useGistUrlParams.ts`

- [ ] **Step 1: 创建 hooks 目录（如不存在）并创建文件**

```typescript
import { useEffect, useRef, useState } from 'react';
import { useSettings } from '@/stores/settingsStore';
import { getSetting } from '@/db/schema';
import type { GistConfig } from '@/stores/settingsStore';

export type GistConflict = {
  urlGistId: string;
  urlToken: string;
  stored: GistConfig;
};

/** 解析 HashRouter URL 中的 ?gistId=&githubToken= 参数。
 *  返回 conflict 对象供调用方显示冲突弹窗；resolved 后调用 resolveConflict()。 */
export function useGistUrlParams() {
  const setGist = useSettings((s) => s.setGist);
  const [conflict, setConflict] = useState<GistConflict | null>(null);
  const handled = useRef(false);

  useEffect(() => {
    if (handled.current) return;
    handled.current = true;

    (async () => {
      // HashRouter: window.location.hash = "#/settings?gistId=xxx&githubToken=yyy"
      const hashPart = window.location.hash; // e.g. "#/settings?foo=bar"
      const qIdx = hashPart.indexOf('?');
      if (qIdx === -1) return;

      const params = new URLSearchParams(hashPart.slice(qIdx + 1));
      const urlGistId = params.get('gistId') ?? '';
      const urlToken = params.get('githubToken') ?? '';
      if (!urlGistId || !urlToken) return;

      // 立即清除 URL 敏感参数
      const cleanHash = hashPart.slice(0, qIdx);
      history.replaceState(null, '', window.location.pathname + window.location.search + cleanHash);

      // 读取 IndexDB 中已存的 GistConfig
      const stored = await getSetting<GistConfig>('gist', {
        gistId: '', token: '', autoSync: false, includeAIConfig: false,
      });

      if (stored.gistId === urlGistId && stored.token === urlToken) {
        // 完全一致，静默忽略
        return;
      }

      if (!stored.gistId) {
        // 未配置过，静默写入
        await setGist({ gistId: urlGistId, token: urlToken });
        // 触发全局 toast（挂在 document 事件上，Settings 页监听）
        window.dispatchEvent(new CustomEvent('gist-url-applied', { detail: { gistId: urlGistId } }));
        return;
      }

      // 已配置且不一致 → 暴露冲突供调用方弹窗
      setConflict({ urlGistId, urlToken, stored });
    })();
  }, [setGist]);

  async function resolveConflict(useUrl: boolean) {
    if (!conflict) return;
    if (useUrl) {
      await setGist({ gistId: conflict.urlGistId, token: conflict.urlToken });
    }
    setConflict(null);
  }

  return { conflict, resolveConflict };
}
```

- [ ] **Step 2: 验证 TypeScript 编译**

```bash
npx tsc --noEmit
```

Expected: 无错误

- [ ] **Step 3: Commit**

```bash
git add src/hooks/useGistUrlParams.ts
git commit -m "feat(gist): add useGistUrlParams hook for URL-based config import"
```

---

## Task 4: 修改 AppLayout — 挂载时调用 hook 并渲染冲突弹窗

**Files:**
- Modify: `src/components/AppLayout.tsx`

- [ ] **Step 1: 在 AppLayout.tsx 中引入 hook 和 MobileSheet（已有）**

在文件顶部 import 区添加：

```typescript
import { useGistUrlParams } from '@/hooks/useGistUrlParams';
```

- [ ] **Step 2: 在 AppLayout 函数体中调用 hook**

在现有 `const [shortcutsOpen, setShortcutsOpen] = useState(false);` 之后添加：

```typescript
const { conflict, resolveConflict } = useGistUrlParams();
```

- [ ] **Step 3: 在 JSX 末尾（`</div>` 闭合前）添加冲突弹窗**

在 `<ShortcutsPanel ... />` 之后、最外层 `</div>` 之前插入：

```tsx
{/* ============ Gist 凭据冲突弹窗 ============ */}
<MobileSheet
  open={!!conflict}
  onClose={() => resolveConflict(false)}
  side="bottom"
  title="cloud sync · 配置冲突"
>
  {conflict && (
    <div className="space-y-4">
      <p className="text-sm text-ink2 leading-relaxed">
        检测到 URL 中携带新的 Gist 配置，与当前设备已存的配置不一致。
      </p>
      <div className="rounded-md border border-paper3 bg-paper2/50 p-3 font-mono text-[11px] space-y-1">
        <div className="text-ink3 uppercase tracking-wider">url 中的配置</div>
        <div>Gist ID: <span className="text-ink">{conflict.urlGistId}</span></div>
        <div>Token: <span className="text-ink">{'*'.repeat(8)}{conflict.urlToken.slice(-4)}</span></div>
      </div>
      <div className="flex flex-col gap-2 sm:flex-row">
        <button
          onClick={() => resolveConflict(true)}
          className="flex-1 rounded-sm border border-ink bg-ink px-4 py-2.5 text-sm font-medium text-paper transition hover:opacity-90"
        >
          使用 URL 中的配置（覆盖）
        </button>
        <button
          onClick={() => resolveConflict(false)}
          className="flex-1 rounded-sm border border-paper3 px-4 py-2.5 text-sm font-medium text-ink2 transition hover:border-ink"
        >
          保留现有配置
        </button>
      </div>
    </div>
  )}
</MobileSheet>
```

- [ ] **Step 4: 验证编译，启动开发服务器手工测试**

```bash
npx tsc --noEmit
npm run dev
```

在浏览器中访问 `http://localhost:5173/#/settings?gistId=test123&githubToken=ghp_test`，确认：
1. URL 参数被立即清除（地址栏变回 `/#/settings`）
2. 未配置时静默写入（Settings 页 Gist ID 输入框出现 `test123`）
3. 已配置且不一致时弹出冲突弹窗

- [ ] **Step 5: Commit**

```bash
git add src/components/AppLayout.tsx
git commit -m "feat(gist): handle URL gist params in AppLayout with conflict dialog"
```

---

## Task 5: Settings 页新增 Gist 同步 Section（使用 frontend-design skill）

**Files:**
- Modify: `src/pages/Settings.tsx`

- [ ] **Step 1: 调用 frontend-design skill 设计 GistSyncSection 组件**

> **重要**：在实施此 Task 时，调用 `frontend-design` skill，传入以下上下文：
>
> - 项目设计系统：persimmon/ink/paper/moss/crimson 色彩，font-mono/font-display，`.input` / `.btn-ghost` / `.btn-accent` CSS 类
> - 现有 Section/Field/Toggle/Checkbox 组件已在 Settings.tsx 末尾定义，可直接复用
> - 移动端优先：按钮在 `<sm` 时 `flex-col`，`sm:` 时 `flex-row`
> - 需要实现的完整 UI 见规格 `docs/superpowers/specs/2026-05-13-gist-sync-design.md` 第 5 节

在等待 frontend-design skill 输出之前，可先完成 Step 2（状态声明）。

- [ ] **Step 2: 在 Settings 组件内新增 Gist 相关状态**

在现有 `const [importing2, setImporting2] = useState(false);` 之后添加：

```typescript
const { gist, setGist } = useSettings();
const [gistPushing, setGistPushing] = useState(false);
const [gistPulling, setGistPulling] = useState(false);
const [gistError, setGistError] = useState('');
const [gistSuccess, setGistSuccess] = useState('');
const [gistPullStrategy, setGistPullStrategy] = useState<ImportStrategy>('merge');
const [gistPullAIConfig, setGistPullAIConfig] = useState(false);
```

- [ ] **Step 3: 在 Settings.tsx 顶部 import 区添加新依赖**

```typescript
import { pushToGist, pullFromGist } from '@/lib/gist';
import { Cloud, CloudUpload, CloudDownload, Unlink } from 'lucide-react';
```

- [ ] **Step 4: 实现推送处理函数**

在 `pullModels` 函数之后添加：

```typescript
async function handleGistPush() {
  if (!gist.token) return;
  setGistPushing(true);
  setGistError('');
  setGistSuccess('');
  try {
    const backup = await buildBackup({
      includeSettings: true,
      includeSessions: true,
      includeWords: false,
      includeAIConfig: gist.includeAIConfig,
    });
    const gistId = await pushToGist(backup, gist);
    await setGist({ gistId, lastSyncedAt: new Date().toISOString(), lastSyncStatus: 'ok' });
    setGistSuccess('推送成功');
  } catch (e) {
    await setGist({ lastSyncStatus: 'error' });
    setGistError((e as Error).message);
  } finally {
    setGistPushing(false);
  }
}

async function handleGistPull() {
  if (!gist.token || !gist.gistId) return;
  setGistPulling(true);
  setGistError('');
  setGistSuccess('');
  try {
    const backup = await pullFromGist(gist);
    if (gistPullStrategy === 'replace' && !confirm('替换模式将清空当前进度、会话与语法进度。确认继续？')) {
      setGistPulling(false);
      return;
    }
    const report = await applyBackup(backup, { strategy: gistPullStrategy, applyAIConfig: gistPullAIConfig });
    await useSettings.getState().load();
    await setGist({ lastSyncedAt: new Date().toISOString(), lastSyncStatus: 'ok' });
    setGistSuccess(`拉取成功 · 进度 ${report.progress} 条 · 设置 ${report.settings} 项`);
  } catch (e) {
    await setGist({ lastSyncStatus: 'error' });
    setGistError((e as Error).message);
  } finally {
    setGistPulling(false);
  }
}

function handleGistDisconnect() {
  if (!confirm('断开配置将清除本设备保存的 Gist ID 和 Token，不影响 Gist 上的数据。确认？')) return;
  setGist({ gistId: '', token: '', lastSyncedAt: undefined, lastSyncStatus: undefined });
  setGistSuccess('');
  setGistError('');
}
```

- [ ] **Step 5: 在 TOC_ITEMS 数组中插入 Gist 条目**

在 `TOC_ITEMS` 数组的最前面插入（使其在导航中排在"学习偏好"之前，对应实际 Section 顺序）：

```typescript
{ id: 'gist', label: '云端同步', en: 'gist sync' },
```

- [ ] **Step 6: 在 JSX 中"数据备份" Section 之前插入 Gist 同步 Section**

将 frontend-design skill 生成的 GistSyncSection JSX 插入到 `{/* 数据备份 */}` 注释行之前。

如 frontend-design skill 返回独立组件，则将其放在 Settings.tsx 末尾（与 `Section`、`Field` 等组件并列），并在此处调用：

```tsx
{/* ============= 云端同步 · GitHub Gist ============= */}
<GistSyncSection
  gist={gist}
  setGist={setGist}
  pushing={gistPushing}
  pulling={gistPulling}
  error={gistError}
  success={gistSuccess}
  pullStrategy={gistPullStrategy}
  setPullStrategy={setGistPullStrategy}
  pullAIConfig={gistPullAIConfig}
  setPullAIConfig={setGistPullAIConfig}
  onPush={handleGistPush}
  onPull={handleGistPull}
  onDisconnect={handleGistDisconnect}
/>
```

如 frontend-design skill 返回内联 JSX，直接插入即可，无需额外 props 传递（内联可直接访问局部状态）。

- [ ] **Step 7: 验证编译和视觉效果**

```bash
npx tsc --noEmit
npm run dev
```

访问 `/#/settings`，确认：
1. TOC 显示"云端同步"条目，点击可跳转
2. Gist ID / Token 输入框可编辑
3. 推送/拉取/断开按钮正常渲染
4. 移动端（Chrome DevTools 375px 宽）按钮纵向堆叠
5. 状态栏（从未同步 / 成功 / 失败）正确显示

- [ ] **Step 8: Commit**

```bash
git add src/pages/Settings.tsx
git commit -m "feat(gist): add Gist sync section to Settings page"
```

---

## Task 6: 学习/复习完成时触发自动推送

**Files:**
- Modify: `src/pages/Learn.tsx`
- Modify: `src/pages/Review.tsx`

- [ ] **Step 1: 修改 Learn.tsx — 导入 autoSyncToGist 和 useSettings**

在 Learn.tsx 顶部 import 区添加：

```typescript
import { autoSyncToGist } from '@/lib/gist';
```

`useSettings` 已导入，无需重复。

- [ ] **Step 2: 在 Learn.tsx 中读取 gist 状态**

在 Learn 函数内，`useSettings` 解构处添加 `gist` 和 `setGist`：

```typescript
const { activeLevel, dailyNewWords, learnOrder, reviewAlgorithm, ai, loaded,
        learningMode, enhanced, gist, setGist } = useSettings();
```

- [ ] **Step 3: 在 Learn.tsx 的 setStage('done') 处触发自动同步**

找到 Learn.tsx 第 264-268 行：

```typescript
if (isEnhanced && enhanced.microReview && newWords.length > 0) {
  setStage('micro');
} else {
  setStage('done');
}
```

修改为：

```typescript
if (isEnhanced && enhanced.microReview && newWords.length > 0) {
  setStage('micro');
} else {
  setStage('done');
  void autoSyncToGist(gist, setGist);
}
```

- [ ] **Step 4: 在 Learn.tsx 的 MicroReview onComplete 处也触发**

找到 Learn.tsx 中 `onComplete={() => setStage('done')}` 这行（约第 499 行），修改为：

```typescript
onComplete={() => { setStage('done'); void autoSyncToGist(gist, setGist); }}
```

- [ ] **Step 5: 修改 Review.tsx — 导入 autoSyncToGist**

在 Review.tsx 顶部 import 区添加：

```typescript
import { autoSyncToGist } from '@/lib/gist';
```

- [ ] **Step 6: 在 Review.tsx 中读取 gist 状态**

在 Review 函数内 `useSettings()` 调用中解构出 `gist` 和 `setGist`（Review.tsx 中当前已使用 useSettings，需确认解构方式）。在 `useSettings` 调用处追加：

```typescript
const gist = useSettings((s) => s.gist);
const setGist = useSettings((s) => s.setGist);
```

- [ ] **Step 7: 在 Review.tsx 批次完成时触发自动同步**

找到 Review.tsx 第 204-211 行（`if (idx + 1 >= items.length)` 块）：

```typescript
if (idx + 1 >= items.length) {
  await sessionsRepo.log({
    type: 'review',
    wordsCount: items.length,
    correctCount: stats.correct + (isCorrect ? 1 : 0),
    durationMs: Date.now() - startTime,
  });
}
```

修改为：

```typescript
if (idx + 1 >= items.length) {
  await sessionsRepo.log({
    type: 'review',
    wordsCount: items.length,
    correctCount: stats.correct + (isCorrect ? 1 : 0),
    durationMs: Date.now() - startTime,
  });
  void autoSyncToGist(gist, setGist);
}
```

- [ ] **Step 8: 验证编译**

```bash
npx tsc --noEmit
```

Expected: 无错误

- [ ] **Step 9: 手工测试自动推送**

1. 在 Settings 页面填入有效 GitHub Token + Gist ID（或空 gistId 让其自动创建）
2. 开启"自动推送"开关
3. 进入学习页完成一轮学习
4. 返回 Settings 页面，确认状态栏显示"上次同步：[时间] ✓ 成功"

- [ ] **Step 10: Commit**

```bash
git add src/pages/Learn.tsx src/pages/Review.tsx
git commit -m "feat(gist): trigger autoSyncToGist on learn/review session completion"
```

---

## Task 7: 端到端验证与收尾

- [ ] **Step 1: 完整编译检查**

```bash
npx tsc --noEmit && npm run build
```

Expected: Build 成功，无 TypeScript 错误

- [ ] **Step 2: 验证 URL 参数引导流程**

访问 `http://localhost:5173/#/?gistId=REAL_GIST_ID&githubToken=REAL_TOKEN`：
- 确认 URL 立即清除敏感参数
- 确认 Settings 页面显示配置已写入

- [ ] **Step 3: 验证推拉功能**

1. 手动点"推送备份"→ 确认 GitHub Gist 页面有新文件 `english-hub-backup.json`
2. 手动点"拉取同步"（合并策略）→ 确认数据正常导入

- [ ] **Step 4: 验证移动端布局**

Chrome DevTools 切换到 375px 宽度，确认：
- 操作按钮纵向堆叠
- 输入框全宽
- 状态栏文字不溢出

- [ ] **Step 5: Final commit**

```bash
git add -A
git commit -m "feat(gist): complete GitHub Gist cloud sync feature"
```
